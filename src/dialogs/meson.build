modulesubdir = join_paths(moduledir, 'dialogs')

services_sources = [
    'save_changes_dialog.py',
    'audio_export_dialog.py',
    'random_beats_dialog.py',
]

install_data(services_sources, install_dir: modulesubdir)