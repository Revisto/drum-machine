preset_dir = meson.current_source_dir()

preset_files = [
  'Boom Boom.mid',
  'Maybe Rock.mid',
  'Night.mid',
  'Slow.mid',
  'Chill.mid',
  'Shoot.mid',
]

install_data(preset_files, install_dir: get_option('datadir') / 'drum-machine' / 'data' / 'presets')